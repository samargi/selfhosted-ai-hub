# === Domain for Caddy (use your no-ip host) =======================
DOMAIN=myhub.ddns.net

# === Core ==========================================================
POSTGRES_USER=hub
POSTGRES_PASSWORD=change_me
POSTGRES_DB=hub

MINIO_ROOT_USER=hubminio
MINIO_ROOT_PASSWORD=change_me_minio

# OpenVPN
CLIENTNAME=my_client_name

# n8n encryption key (generate a long random string)
N8N_ENCRYPTION_KEY=change_me_long_random

# OpenAI key (optional, if using external API via LiteLLM)
OPENAI_API_KEY=

# === Messaging integrations (optional) =============================
# Slack
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=

# Microsoft Graph / Teams
MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# Telegram
TELEGRAM_BOT_TOKEN=

# IMAP (email intake)
IMAP_HOST=
IMAP_USER=
IMAP_PASS=
IMAP_TLS=true

PIPELINES_API_KEY=